# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
# `dist/` is committed on purpose: Cloudflare Pages serves the dashboard
# without running a build, so the compiled output has to live in the repo.
# Rebuild with the production API base URL before committing:
#   VITE_API_BASE_URL=https://api.de-von.uz/api/v1 npm run build
dist-ssr
*.local
.env.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
